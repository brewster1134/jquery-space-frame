!!! 5
%html
  %head
    %meta{ charset : "utf-8" }
    %title Space Frame Test Suite
    / Load local jQuery. This can be overridden with a ?jquery=___ param.
    %script{ src : "../libs/jquery-loader.js" }
    / Load local QUnit (grunt requires v1.0.0 or newer).
    %link{ href : "../libs/qunit/qunit.css", media : "screen", rel : "stylesheet" }
    %script{ src : "../libs/qunit/qunit.js" }
    / Load local lib and tests.
    %script{ src : "../dist/spaceFrame.js" }
    %script{ src : "test.js" }
    /
      Removing access to jQuery and $. But it'll still be available as _$, if
      you REALLY want to mess around with jQuery in the console. REMEMBER WE
      ARE TESTING YOUR PLUGIN HERE
    :javascript
      window._$ = jQuery.noConflict(true);
  %body
    .space-frame#four_panel
      .space-scrubber
      .space-panel
      .space-panel
      .space-panel
      .space-panel

    .space-frame.x#two_panel_left_right
      .space-scrubber
      .space-panel
      .space-panel

    .space-frame.y#two_panel_top_bottom
      .space-scrubber
      .space-panel
      .space-panel
